# Simple Live Server

Minimal Node.js live reload server for `.html` and `.css` files using HTTP + WebSocket.

---

## Features

- Serves HTML and CSS
- Live reload on file change
- HTML → full reload
- CSS → hot reload (no refresh)
- WebSocket based
- Auto script injection

---

## Setup

```bash
npm install
node server.js
